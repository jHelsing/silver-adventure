package se.jonteh.ecoapp;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

/**
 * Simple test just to ensure that the CI pipeline and various static analytics are working.
 * Remove as soon as real tests exist!
 */
class ApplicationTest {

  @Test
  void testSomething() {
    assertEquals(8, 4 * 2);
  }
}
